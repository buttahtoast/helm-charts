apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ref.fullname" . }}-test-connection"
  labels:
    {{- include "ref.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ref.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
